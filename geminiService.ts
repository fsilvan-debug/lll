
// שירות ה-AI מנוטרל זמנית לבקשת המשתמש כדי למנוע שגיאות "מפתח לא נמצא"
export const askAboutPlanet = async (planetName: string, question: string) => {
  return "שירות ה-AI אינו זמין כרגע. כל המידע המדעי מופיע בכרטיסיות המידע.";
};
